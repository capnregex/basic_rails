<!DOCTYPE html>
<html>
  <head>
    <title>BasicRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <header>
      <a href="/">Basic Rails</a>
      <%= link_to 'Meat', meats_path %>
    </header>
    <%= yield %>
  </body>
</html>
